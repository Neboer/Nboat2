doctype html
html
    head
        meta(charset="utf-8")
        meta(http-equiv="X-UA-Compatible" content="IE=edge")
        meta(name="viewport" content="width=device-width, initial-scale=1")
        block title_and_libs
            title Neboer's Boat - {{ current }}
            script(src="library/jquery-3.4.1.min.js")
            script(src="library/popper.min.js")
            script(src="library/bootstrap.min.js")
            link(rel="stylesheet" href="library/bootstrap.min.css")
            script(src="library/font_awesome.js")
    body
        .biggest
            header.py-lg-2
                .row.flex-nowrap.justify-content-between.align-items-center
                    .text-center.mx-auto
                        h1.large-op Neboer's Boat
                        small Neboer's blog isn't only about technique!
                    .icon-plate
                        a.text-muted.alinement
                            i.fab.fa-github
                        a.text-muted.alinement
                            i.fas.fa-envelope
                        a.text-muted.alinement
                            i.fas.fa-search
            block page_menu

            block content


    block style
        style.
            .alinement {
                margin-right: .5em;
            }
            .banner {
                max-width: 100%;
            }
            .biggest {
                width: 75%;
                margin: auto;
            }
            .icon-plate {
                position: absolute;
                right: 13%;
            }
            @media (max-width: 576px) {
                .biggest {
                    width: 98%;
                    margin: auto;
                }
                .icon-plate {
                    position: absolute;
                    right: 0;
                    display: flex;
                    flex-direction: column;
                }
            }
